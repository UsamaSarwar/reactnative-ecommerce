import {
  StyleSheet,
  Text,
  View,
  SafeAreaView,
  TextInput,
  Alert,
} from "react-native";

import AppNavigator from "./app.navigator";

import { useState } from "react";
export default function App() {
  return <AppNavigator />;
}
