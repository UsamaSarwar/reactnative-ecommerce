import { StyleSheet } from "react-native";

export default TextStyles = StyleSheet.create({
  error_message: { fontSize: 20, color: "red", padding: 10 },
  name_banner: {
    fontSize: 20,
    marginTop: 10,
    fontWeight: "bold",
  },
  userName_banner: {
    fontSize: 15,
    textShadowColor: "rgba(66, 200, 143, 0.4)",
    textShadowOffset: { width: -1, height: 1 },
    textShadowRadius: 15,
  },
});
